<% content_for :user, :user %>

<%= t("mail.articles.html", name: (@user.name ? @user.name : @user.email), 
                            link: link_to(@article.get_title, url_for(controller: :articles, action: :show, id: @article.id))) %>