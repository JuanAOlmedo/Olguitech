<%= form_with model: @super_category do |form| %>
    <div class='form'>
        <div class="field">
            <%= form.label :title, 'Título' %>
            <%= form.text_area :title %>
        </div>

        <div class="field">
            <%= form.label :title2, 'Título en inglés' %>
            <%= form.text_area :title2 %>
        </div>

        <%= form.submit 'Crear', class: 'btn centered' %>
    </div>
<% end %>
