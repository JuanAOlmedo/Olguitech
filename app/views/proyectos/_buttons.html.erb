<%= link_to "Editar", edit_proyecto_path(article), class: 'btn' unless article.trashed? %>

<% if article.drafted? %>
    <%= link_to "Publicar", proyecto_path(article, params: { proyecto: { status: 0 } }), data: { turbo_method: :patch }, class: 'btn' %>
<% elsif !article.trashed? %>
    <%= link_to "Despublicar", proyecto_path(article, params: { proyecto: { status: 1 } }), data: { turbo_method: :patch }, class: 'btn' %>
<% end %>

<% if article.trashed? %>
    <%= link_to "Sacar de la papelera", proyecto_path(article, params: { proyecto: { status: 1 } }), data: { turbo_method: :patch }, class: 'btn' %>
<% else %>
    <%= link_to "Mover a la papelera", proyecto_path(article, params: { proyecto: { status: 2 } }), data: { turbo_method: :patch }, class: 'btn is-danger' %>
<% end %>
