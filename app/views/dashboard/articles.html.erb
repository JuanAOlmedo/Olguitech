<div class='dashboard-holder'>
    <%= render 'dashboard' %>

    <div class='dashboard' data-controller='dashboard'>
        <h1 class='main-title'> Todos los artículos </h1>
        <div class="dropdown-holder" style="height: 2.5rem; display: flex;">
            <%= dropdown_for 'Nuevo' do %>
                <%= link_to Solution.model_name.human, new_solution_path, class: 'btn' %>
                <%= link_to Project.model_name.human, new_project_path, class: 'btn' %>
                <%= link_to Product.model_name.human, new_product_path, class: 'btn' %>
                <%= link_to Category.model_name.human, new_category_path, class: 'btn' %>
                <%= link_to Newsletter.model_name.human, new_newsletter_path, class: 'btn' %>
            <% end %>
        </div>

        <h2 class="title centered"> Borradores </h2>
        <div class='dashboard-drafts'>
            <% if @solution_drafts.any? %>
                <h2 class="title centered"> Soluciones (borradores) </h2>
                <div class='dashboard-card-holder' id='drafted_solutions'>
                    <% @solution_drafts.each do |article| %>
                        <%= render partial: 'article', object: article %>
                    <% end %>
                </div>
            <% end %>

            <% if @project_drafts.any? %>
                <h2 class="title centered"> Proyectos (borradores) </h2>
                <div class='dashboard-card-holder' id='drafted_projects'>
                    <% @project_drafts.each do |article| %>
                        <%= render partial: 'article', object: article %>
                    <% end %>
                </div>
            <% end %>

            <% if @product_drafts.any? %>
                <h2 class="title centered"> Productos (borradores) </h2>
                <div class='dashboard-card-holder' id='drafted_products'>
                    <% @product_drafts.each do |article| %>
                        <%= render partial: 'article', object: article %>
                    <% end %>
                </div>
            <% end %>

            <% if [ @solution_drafts, @product_drafts, @project_drafts ].flatten.empty? %>
                <h2 id='dashboard-message'> No hay ningún borrador </h2>
            <% end %>
        </div>

        <% if @solutions.any? %>
            <h2 class="title centered"> Soluciones publicadas </h2>
            <div class='dashboard-card-holder' id='published_solutions'>
                <% @solutions.each do |article| %>
                    <%= render partial: 'article', object: article %>
                <% end %>
            </div>
        <% end %>

        <% if @projects.any? %>
            <h2 class="title centered"> Proyectos publicados </h2>
            <div class='dashboard-card-holder' id='published_projects'>
                <% @projects.each do |article| %>
                    <%= render partial: 'article', object: article %>
                <% end %>
            </div>
        <% end %>

        <% if @products.any? %>
            <h2 class="title centered"> Productos publicados </h2>
            <div class='dashboard-card-holder' id='published_products'>
                <% @products.each do |article| %>
                    <%= render partial: 'article', object: article %>
                <% end %>
            </div>
        <% end %>
    </div>
</div>
