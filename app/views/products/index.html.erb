<% content_for :title, "#{I18n.t 'shared.our_products'} | Olguitech s.a.s." %>

<%= tag.h1 t('shared.our_products'), class: 'main-title' %>

<% if @super_category %>
    <%= render @super_category, super_categories: @super_categories, model_name: :products %>
<% else %>
    <%= alternative_cards_for Product.all %>
<% end %>

