Olguitech s.a.s.
=============================================

<%= t 'mail.contact.greeting', name: (@user.name || @user.email) %>

<%= t('mail.contact.attachments') %>

<% @contact.interests.each do |interest| %>
    <%= interest.record.localized_title %> (<%= url_for interest.record %>)
<% end %>

<%= t('mail.contact.message', message: @contact.message) %>

<%= @user.company ? t('mail.contact.final_company', company: @user.company) : t('mail.contact.final') %>
