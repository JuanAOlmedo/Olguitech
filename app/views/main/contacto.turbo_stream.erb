<%= turbo_stream.replace 'message-form', partial: 'main/message_form' %>

<%= turbo_stream.replace 'recaptcha' do %>
    <div class='field separate'
         id='recaptchaV2'
         data-controller='recaptcha-v2'
         data-recaptcha-v2-site-key-value="<%= Rails.application.credentials.RECAPTCHA_SITE_KEY_V2 %>">
    </div>
<% end %>