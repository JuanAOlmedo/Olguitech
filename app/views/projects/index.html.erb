<% content_for :title, "#{I18n.t 'shared.our_projects'} | Olguitech s.a.s." %>

<%= tag.h4 t('shared.our_projects'), class: 'main-title' %>

<% if @super_category %>
    <%= render @super_category, super_categories: @super_categories, model_name: :projects %>
<% else %>
    <%= cards_for Project.all %>
<% end %>

